{
    "scripts": {
        "build": "node scripts/build.mjs",
        "format": "prettier --write ."
    },
    "dependencies": {
        "esbuild": "^0.27.2"
    },
    "devDependencies": {
        "@types/chrome": "^0.1.32",
        "prettier": "^3.7.4"
    }
}
